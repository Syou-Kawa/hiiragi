{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath // empty' | { read -r f; case \"$f\" in \"$CLAUDE_PROJECT_DIR\"/frontend/*|\"$CLAUDE_PROJECT_DIR\"/api/*) \"$CLAUDE_PROJECT_DIR\"/scripts/sync-portfolio-site.sh 2>/dev/null || true;; esac; }",
            "statusMessage": "hiiragi-portfolio-site へ同期中..."
          }
        ]
      }
    ]
  }
}
